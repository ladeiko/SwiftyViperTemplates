{% include 'header' %}

// Navigation routes for this module's NavigationStack. Each case is resolved to
// a child module by {{ prefix }}{{ module_info.name }}Router.destination(for:).
enum {{ prefix }}{{ module_info.name }}Route: Hashable {
    // TODO: Replace this placeholder with your real destinations,
    //       e.g. case detail(id: String)
    case placeholder
}
