{% include 'header' %}

class {{ prefix }}{{ module_info.name }}ViewLayout: UICollectionViewFlowLayout {
    
    init() {
        super.init()
        //sectionInset = UIEdgeInsets(top: 20, left: 10, bottom: 10, right: 10)
        //itemSize = CGSize(width: 60, height: 60)
        // TODO: Place your code here
    }
    
}
