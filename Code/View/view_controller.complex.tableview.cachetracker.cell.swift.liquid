{% include 'header' %}

import UIKit

class {{ prefix }}{{ module_info.name }}ViewCell: UITableViewCell {

    // MARK: Outlets
    // TODO

    // MARK: Vars
    // TODO

    // MARK: Public

    func configure(with model: {{ prefix }}{{ module_info.name }}ModuleEntityModel) {
        // TODO
    }

    func willDisplay(_ model: {{ prefix }}{{ module_info.name }}ModuleEntityModel) {
        module.embdedInView(contentView)
    }

    func stopDisplay(_ model: {{ prefix }}{{ module_info.name }}ModuleEntityModel) {
        module.removeFromView()
    }

    // MARK: Helpers
    // TODO
    
}
