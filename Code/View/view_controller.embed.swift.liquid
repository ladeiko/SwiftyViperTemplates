{% include 'header' %}

import UIKit
import RamblerSegues

class {{ prefix }}{{ module_info.name }}ViewController: UIViewController, RamblerEmbedSegueViewContainer, {{ prefix }}{{module_info.name}}ViewInput {

    // MARK: VIPER Vars

    var output: {{ prefix }}{{ module_info.name }}ViewOutput!

    // MARK: Outlets
    // TODO

    // see https://github.com/rambler-digital-solutions/RamblerSegues
    @IBOutlet weak var embedSomeModuleSegueContainer: UIView!

    // MARK: Vars
    // TODO

    // MARK: Life cycle

    override func viewDidLoad() {
        super.viewDidLoad()
        output.viewIsReady()
    }

    // MARK: {{ prefix }}{{module_info.name}}ViewInput
    // TODO

    // MARK: UI Actions
    // TODO

    // MARK: RamblerEmbedSegueViewContainer

    func view(forEmbedIdentifier embedIdentifier: String!) -> UIView! {
        switch embedIdentifier {
        // see https://github.com/rambler-digital-solutions/RamblerSegues    
        case "EmbedSomeModuleSegue":
            return self.embedSomeModuleSegueContainer
        default:
            return nil
        }
    }

    // MARK: Helpers
    // TODO
}
