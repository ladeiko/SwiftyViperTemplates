{% include 'header' %}

//import UIKit

@MainActor
protocol {{ prefix }}{{ module_info.name }}ViewOutput: AnyObject {
    func viewIsReady()
    // func viewNeedsImage(for model: {{ prefix }}{{ module_info.name }}ModuleEntityModel) async -> UIImage?
}
