{% include 'header' %}

import UIKit

typealias {{ prefix }}{{ module_info.name }}EmbeddedModule = SomeEmbeddedModule

protocol {{ prefix }}{{ module_info.name }}EmbeddableModule {

    var embedInView: ((_ view: UIView) -> Void) { get set }
    var removeFromView: (() -> Void)  { get set }

}

