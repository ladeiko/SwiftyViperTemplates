{% include 'header' %}

import UIKit

typealias {{ prefix }}{{ module_info.name }}EmbeddableRemoveBlock = () -> Void
typealias {{ prefix }}{{ module_info.name }}EmbeddableEmbedBlock = (_ containerView: UIView) -> {{ prefix }}{{ module_info.name }}EmbeddableRemoveBlock

protocol {{ prefix }}{{ module_info.name }}Embeddable {

    var embed: {{ prefix }}{{ module_info.name }}EmbeddableEmbedBlock! { get }

}
