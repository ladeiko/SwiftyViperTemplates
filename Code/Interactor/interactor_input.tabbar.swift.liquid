{% include 'header' %}

import Foundation

protocol {{ prefix }}{{ module_info.name }}InteractorInput: AnyObject {
    {% if custom_parameters.extended_configure %}func configure(with {% if custom_parameters.split_module_config == "true" -%}
                                                                    config: {{ prefix }}{{ module_info.name }}InteractorConfig)
                                                                    {%- else -%}
                                                                    config: {{ prefix }}{{ module_info.name }}ModuleConfig)
                                                                    {%- endif -%}{% else %}func configure(){% endif %}
    func deinitialize()
    var currentTab: {{ prefix }}{{ module_info.name }}ModuleTab { get set }
}
