{% include 'header' %}

import Foundation

class {{ prefix }}{{ module_info.name }}Interactor: {{ prefix }}{{ module_info.name }}InteractorInput {

    // MARK: - VIPER Vars

    weak var output: {{ prefix }}{{ module_info.name }}InteractorOutput!

    // MARK: - Vars
    
    {% if custom_parameters.extended_configure %}private var {% if custom_parameters.split_module_config == "true" -%}
                                                            config: {{ prefix }}{{ module_info.name }}InteractorConfig!
                                                            {%- else -%}
                                                            config: {{ prefix }}{{ module_info.name }}ModuleConfig!
                                                            {%- endif -%}{%- endif %}
    {% if custom_parameters.split_module_config == "true" -%}
    private lazy var services: {{ prefix }}{{ module_info.name }}InteractorServices = { config.servicesFactory.servicesFor{{ prefix }}{{ module_info.name }} }()
    {%- endif %}

    {% if custom_parameters.use_storyboard == "false" and custom_parameters.split_module_config == "true" -%}
    init(servicesFactory: servicesFactory) {

    }
    {%- endif %}

    // MARK: - {{ prefix }}{{ module_info.name }}InteractorInput
    
    {% include 'interactor_configure.swift' %}

    {% include 'interactor_deinitialize.swift' %}

    // MARK: - Helpers
    // TODO: Place your code here

//    func someAction() {
//        guard let output = output else { return }
//        output.someInteractorAction()
//    }    
}
